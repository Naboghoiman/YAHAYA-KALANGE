export interface DiscDjDeck {  originalBpm: number;  beatPeriod: number;  beatStart: number;  getSpeed: () => number;  setSpeed: (speed: number) => void;  getCurrentPosition: () => number;  seekTo: (positionSeconds: number) => void;  isPlaying: () => boolean;  start: () => void;  setResetSpeedFlag: (enabled: boolean) => void;}function multiplicativeDistance(a: number, b: number): number {  if (a <= 0.0 || b <= 0.0) return Infinity;  return a > b ? a / b : b / a;}function nextFourBeatBoundary(currentPosition: number, beatStart: number, sourceCycle: number): number {  if (sourceCycle <= 0.0) return currentPosition;  const relative = currentPosition - beatStart;  let remainder = relative % sourceCycle;  if (remainder < 0.0) remainder += sourceCycle;  const cycles = Math.floor((currentPosition - beatStart) / sourceCycle);  let boundary = beatStart + cycles * sourceCycle;  while (boundary < currentPosition) boundary += sourceCycle;  return boundary;}function chooseTempoFamily(referenceEffectiveBpm: number, followerEffectiveBpm: number): number {  const candidates = [    referenceEffectiveBpm * 0.5,    referenceEffectiveBpm,    referenceEffectiveBpm * 2.0  ];  let selected = candidates[0];  let bestDistance = multiplicativeDistance(selected, followerEffectiveBpm);  for (let i = 1; i < 3; ++i) {    const distance = multiplicativeDistance(candidates[i], followerEffectiveBpm);    if (distance < bestDistance) {      bestDistance = distance;      selected = candidates[i];    }  }  return selected;}function foldFollowerSpeed(speed: number): number {  if (speed < 0.5) speed *= 2.0;  if (speed > 2.0) speed /= 2.0;  return speed;}export function discDjStyleSync(  direction: number,  deckT: DiscDjDeck,  deckC: DiscDjDeck,  audioBufferFrames: number,  sampleRate: number): boolean {  if (deckT.originalBpm <= 0.0 || deckC.originalBpm <= 0.0) return false;  deckT.setResetSpeedFlag(false);  deckC.setResetSpeedFlag(false);  let master: DiscDjDeck;  let follower: DiscDjDeck;  if (direction === 0) {    master = deckC;    follower = deckT;  } else {    master = deckT;    follower = deckC;  }  const masterEffective = master.originalBpm * master.getSpeed();  const followerEffective = follower.originalBpm * follower.getSpeed();  const selectedEffectiveTempo = chooseTempoFamily(masterEffective, followerEffective);  let targetFollowerSpeed = selectedEffectiveTempo / follower.originalBpm;  targetFollowerSpeed = foldFollowerSpeed(targetFollowerSpeed);  follower.setSpeed(targetFollowerSpeed);  if (deckT.beatPeriod <= 0.0 || deckC.beatPeriod <= 0.0) return true;  const speedT = deckT.getSpeed();  const speedC = deckC.getSpeed();  const realCycleT = (4.0 * deckT.beatPeriod) / speedT;  const realCycleC = (4.0 * deckC.beatPeriod) / speedC;  const commonRealCycle = Math.max(realCycleT, realCycleC);  const cycleTSource = commonRealCycle * speedT;  const cycleCSource = commonRealCycle * speedC;  const posT = deckT.getCurrentPosition();  const posC = deckC.getCurrentPosition();  const nextT = nextFourBeatBoundary(posT, deckT.beatStart, cycleTSource);  const nextC = nextFourBeatBoundary(posC, deckC.beatStart, cycleCSource);  const deltaT = nextT - posT;  const deltaC = nextC - posC;  if (sampleRate <= 0.0) throw new Error("sampleRate must be > 0");  const bufferTerm = audioBufferFrames / sampleRate;  if (direction === 0) {    const followerRealRemaining = (deltaT + bufferTerm) / speedT;    const masterRealRemaining = deltaC / speedC;    const realTimingDifference = followerRealRemaining - masterRealRemaining;    let targetT = posT + realTimingDifference * speedT;    if (targetT < posT && targetT < posT - 0.3 * cycleTSource) {      targetT += cycleTSource;    }    if (targetT < 0.0) targetT += cycleTSource;    deckT.seekTo(targetT);    if (deckC.isPlaying() && !deckT.isPlaying()) {      deckT.start();    }  } else {    const followerRealRemaining = (deltaC + bufferTerm) / speedC;    const masterRealRemaining = deltaT / speedT;    const realTimingDifference = followerRealRemaining - masterRealRemaining;    let targetC = posC + realTimingDifference * speedC;    if (targetC < posC && targetC < posC - 0.3 * cycleCSource) {      targetC += cycleCSource;    }    if (targetC < 0.0) targetC += cycleCSource;    deckC.seekTo(targetC);    if (deckT.isPlaying() && !deckC.isPlaying()) {      deckC.start();    }  }  return true;}
